<h1>Activities#show</h1>
<p>Find me in app/views/activities/show.html.erb</p>

<dl>
  <% @entries.each do |entry| %>
    <dt><%= link_to entry.title, entry.url, target: :_blank %></dt>
    <dd><%= entry.author.uid %></dd>
    <dd><%== entry.content %></dd>
  <% end %>
</dl>
